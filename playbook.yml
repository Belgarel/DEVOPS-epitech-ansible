---
- import_playbook: hosts.yml

- hosts: prodfront
  roles:
    - front
  vars:
    ansible_become_password: "{{ root_password }}"
  tags: front

- hosts: prodapp
  roles:
    - app
  vars:
    ansible_become_password: "{{ root_password }}"
  tags: app

- hosts: prodbdd
  roles:
    - mysql
  vars:
    ansible_become_password: "{{ root_password }}"
  tags: bdd
